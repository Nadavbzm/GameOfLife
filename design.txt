Create cell class - live or dead.
board of cells
cell cellBoard[][]
int displayBoard[][]

live cells have:
getNeighbors(board): //Checks 8 surrounding neighbors
x = 
y = 
int nextGen(board, numNeighbors) 
{
this.x
this.y
needs 2 or 2 neighbors to be alive
else returns false
}

dead cells have:
color: black
getNeighbors(board): //Checks 8 surrounding neighbors
x = 
y = 
int nextGen(board, numNeighbors)// will return if cell lives or dies this turn
{
this.x
this.y
needs 3 neighbors to return true
else returns false
}

board[][] - white or black


main{
for(y<len)
  for(x<width)
   displayBoard[y][x] = cellBoard[y][x].nextGen(cellBoard)



